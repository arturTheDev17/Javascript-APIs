class Image {
  constructor(id, titulo, referencia, extensao, id_user) {
    this.id = id;
    this.titulo = titulo;
    this.referencia = referencia;
    this.extensao = extensao;
    this.id_user = id_user;
  }
}

module.exports = Image;
